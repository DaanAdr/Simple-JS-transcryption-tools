# Simple-JS-transcryption-tools
- Increment keystream index even if the value is unused for the current character (as an option)
- Use Beaufort cipher for progressive shift cipher (as an option)
- Organize grids by keyword desc for columnar cipher (as an option)
- Use keyword for the rows instead of the columns for Columnar cipher (As an option)